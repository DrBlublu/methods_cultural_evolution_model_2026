# methods_cultural_evolution_model_2026


## ENV

Only requires numpy, pandas, matplotlib & seaborn!

To recreate the env, run this command:
conda env create --name recoveredenv --file environment.yml